import { RegisterForm } from "@/components/auth/RegisterForm";

export const metadata = {
  title: "Sign Up - LinkShort",
  description: "Create your LinkShort account",
};

export default function RegisterPage() {
  return <RegisterForm />;
}
